<section class="m-2 flex gap-2" id="project-control-btns">
    <div><%= link_to 'Edit', edit_project_path(@project), class: 'btn' %></div>
    <div><%= link_to 'Delete',
                    project_path(@project),
                    class: 'btn',
                    data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure?"
                    } %></div>
</section>